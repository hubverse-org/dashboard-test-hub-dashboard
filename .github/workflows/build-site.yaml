name: "Rebuild Site"
on:
  workflow_dispatch:
    inputs:
      publish:
        description: "Push Data to branch (uncheck for a dry run)"
        type: boolean
        required: false
        default: true
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  update-site-builder-info:
    name: "Record site-builder version"
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/build-provenance
        with:
          phase: site
          github-token: ${{ secrets.GITHUB_TOKEN }}

  site:
    needs: [update-site-builder-info]
    uses: hubverse-org/hub-dashboard-control-room/.github/workflows/generate-site.yaml@main
    with:
      owner: '${{ github.event.repository.owner.login }}'
      name: '${{ github.event.repository.name }}'
      slug: '${{ github.event.repository.owner.login }}'
      email: '${{ github.event.repository.owner.id }}+${{ github.event.repository.owner.login }}@users.noreply.github.com'
      publish: ${{ inputs.publish || github.event_name == 'push' }}
    secrets:
      id: 'none'
      key: ${{ secrets.GITHUB_TOKEN }}
